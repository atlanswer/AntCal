{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "!api/**"],
      "outputs": [".vinxi/**", ".vercel/**"]
    }
  }
}
