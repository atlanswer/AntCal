{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "!api/**"],
      "outputs": [".vinxi/**", ".vercel/**"]
    }
  }
}
