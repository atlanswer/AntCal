{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "dev": {
      "env": ["VITE_API_URL"]
    },
    "build": {
      "env": ["VITE_API_URL"],
      "inputs": ["$TURBO_DEFAULT$", "!api/**"],
      "outputs": [".vinxi/**", ".vercel/**"]
    }
  }
}
