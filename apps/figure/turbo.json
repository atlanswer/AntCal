{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "dev:api": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "!api/**"],
      "outputs": [".vinxi/**", ".vercel/**"]
    }
  }
}
