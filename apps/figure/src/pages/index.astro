---
import BaseLayout from "~/layouts/Base.astro";

const globRes = import.meta.glob("~/pages/*.{astro,md}");
---

<BaseLayout title="Figure">
  {Object.keys(globRes).map((key) => <p>{key}</p>)}
</BaseLayout>
